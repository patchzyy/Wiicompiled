# Retro Rewind pack hash. Add the distribution zip to the store with:
#
#   nix-store --add-fixed sha256 --recursive RetroRewind6.zip
#
# then paste the printed hash below to enable the Retro Rewind product. An
# empty string keeps the flake building the base product only.
{
  hash = "";
}
